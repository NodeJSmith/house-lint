# Validation Report: 003-built-in-token-defaults

**Verdict: PASS**

## Step 1: Requirements Extracted from design.md

### Functional Requirements (7 total)

| ID | Text (abbreviated) | Section |
|---|---|---|
| FR#1 | HSL101 ships three built-in token families: "spec" (AC, FR, NFR, WP), "task" (T), "known-issues" (KI) | Functional Requirements |
| FR#2 | HSL101 selected + no user `tokens` configured → built-ins are active vocabulary, no `ConfigError` | Functional Requirements |
| FR#3 | HSL101 selected + user `tokens` configured → final vocabulary is union of built-ins and user families | Functional Requirements |
| FR#4 | `hash` field replaced by `separator` (`none`/`hash`/`hash-optional`/`dash`/`dash-optional`) | Functional Requirements |
| FR#5 | "known-issues" family matches `KI-NNN`, separator `dash`, digits 1-4, no suffix | Functional Requirements |
| FR#6 | "spec" family matches AC/FR/NFR/WP, separator `hash-optional`, digits 1-4, suffix `optional-lower-alpha` | Functional Requirements |
| FR#7 | "task" family matches T tokens same as spec + `not_followed_by_time=true` | Functional Requirements |

### Acceptance Criteria (5 total)

| ID | Text (abbreviated) | Section |
|---|---|---|
| AC#1 | `extend-select = ["HSL101"]` with no HSL101 table produces findings for AC/FR/NFR/WP/T/KI in comments/docstrings/filenames | Acceptance Criteria |
| AC#2 | User `tokens` (e.g. JIRA family) alongside built-ins → both JIRA and built-in tokens detected | Acceptance Criteria |
| AC#3 | `uv run pytest` passes with all new and updated tests | Acceptance Criteria |
| AC#4 | `uv run pyright` passes (strict, scoped to `src/`) | Acceptance Criteria |
| AC#5 | `uv run ruff check .` passes | Acceptance Criteria |

**Format validation:** All 7 FR identifiers match `^FR#\d+$`; all 5 AC identifiers match `^AC#\d+$`. No violations found. IDs are sequential with no gaps (FR#1-FR#7, AC#1-AC#5).

## Step 2: Claims Extracted from Task Files

### T01-data-model-and-built-ins.md
- `task_id`: T01, `depends_on`: [], `implements`: ["FR#1", "FR#2", "FR#4"]
- Verify items: FR#1, FR#2, FR#4 — all three present and worded as checkable assertions (specific constants/behavior, not vague adjectives)
- Target Files: `src/house_lint/config.py` (modify), `tests/unit/test_config.py` (modify)
- Prompt cites specific line numbers (34, 37, 47, 272, 305-307, 321-330, 333, 144-145, 437-438) — all verified against current `config.py` and match exactly (see Step 4).

### T02-separator-regex-and-detection.md
- `task_id`: T02, `depends_on`: ["T01"], `implements`: ["FR#5", "FR#6", "FR#7"]
- Verify items: FR#5, FR#6, FR#7 — all present, specific (named example strings that must/must-not match)
- Target Files: `src/house_lint/rules/spec_tokens.py`, `tests/unit/rules/test_spec_tokens.py`, `tests/unit/test_suppressions.py`
- Prompt cites line numbers (134, 140, 147) — verified against current `spec_tokens.py`, exact match.

### T03-integration-and-docs.md
- `task_id`: T03, `depends_on`: ["T01", "T02"], `implements`: ["FR#3", "AC#1", "AC#2", "AC#3", "AC#4", "AC#5"]
- Verify items: FR#3, AC#1, AC#2, AC#3, AC#4, AC#5 — all present
- Target Files: `tests/integration/test_cli.py`, `docs/configuration.md`, `README.md`, `docs/rules.md`, plus a `read:` of design.md
- Prompt cites `test_cli.py:597` (verified — that line falls inside the cited reference test's pyproject.toml fixture, an accurate pattern pointer) and doc line numbers that are close to but not exactly matching current file state (see Step 4 notes).

### context.md — Required Sections

| Section | Present? |
|---|---|
| `## Problem & Motivation` | Yes |
| `## Visual Artifacts` | Yes ("None.") |
| `## Key Decisions` | Yes (5 numbered decisions) |
| `## Constraints & Anti-Patterns` | Yes |
| `## Design Doc References` | Yes |
| `## Convention Examples` (optional) | Yes, present |

All required sections present; context.md is complete.

## Step 3: Traceability Matrix

| Requirement | Implementing Task(s) | Verify Item Present? |
|---|---|---|
| FR#1 | T01 | Yes |
| FR#2 | T01 | Yes |
| FR#3 | T03 (T01 also adds a supporting unit test, not claimed in its own Verify) | Yes (T03) |
| FR#4 | T01 | Yes |
| FR#5 | T02 | Yes |
| FR#6 | T02 | Yes |
| FR#7 | T02 | Yes |
| AC#1 | T03 | Yes |
| AC#2 | T03 | Yes |
| AC#3 | T03 | Yes |
| AC#4 | T03 | Yes |
| AC#5 | T03 | Yes |

Every FR and AC maps to exactly one task's `implements` list, and every mapped requirement has a corresponding `- [ ] FRxx/ACxx:` line in that task's Verify section. No requirement is claimed by zero tasks; no requirement is claimed redundantly in a way that creates ambiguity about ownership.

## Step 4: Problems Identified

### Coverage Gaps (FR/AC)
None. All 7 FRs and 5 ACs are covered by exactly one task each, with a matching Verify item.

### Contradictions
None found. Cross-checked task Prompt text against design.md's `## Architecture`, `## Replacement Targets`, and the actual current source (`config.py`, `spec_tokens.py`):
- `TokenFamily` field replacement (`hash` → `separator`), `kw_only=True`, `BUILTIN_*` constant shapes, `HSL101Options.tokens` default change, `_rule_options` merge order (concatenate then enforce `MAX_TOKEN_FAMILIES`), and the `_SEP_REGEX` dict all match the design doc's Architecture section verbatim.
- Every line number cited in T01's Prompt (34, 37, 47, 272, 275-288, 305-307, 321-330, 333, 341, 144-145, 437-438) was checked against the current `src/house_lint/config.py` and matches exactly.
- Every line number cited in T02's Prompt (134, 140, 147) was checked against the current `src/house_lint/rules/spec_tokens.py` and matches exactly.
- Existing test names cited in T01 (`test_default_config_extend_select_still_enforces_hsl101_token_requirement`, `test_default_config_rejects_cli_hsl101_without_tokens`, `test_hsl101_requires_tokens_only_when_selected`, `test_token_family_is_typed_and_validated`) all exist at the cited approximate locations in `tests/unit/test_config.py` and their current bodies match the described pre-change behavior (e.g. `test_token_family_is_typed_and_validated` currently asserts `tokens[0].hash == "optional"` against a `hash = "optional"` TOML fixture — exactly what T01 says to replace).
- `TokenFamily(...)` call sites cited as needing keyword conversion in T02 (`test_spec_tokens.py` lines ~22-27, ~47) and T02's `test_suppressions.py` positional calls (lines 82, 114) were confirmed present as positional calls in the current source.

### Vague Verification Criteria
None. All Verify items name concrete constants, function calls, expected exceptions, or example strings (e.g. "`separator="dash"` matches `KI-001` but not `KI001` or `KI#001`", "`default_config(cli_extend_select=("HSL101",))` returns a `LintConfig` with `hsl101.tokens` containing the three built-in families").

### Non-Local Verification Criteria
None problematic. T01 and T02 Verify items are checkable entirely within their own target files. T03's AC#3-AC#5 Verify items require running repo-wide commands (`uv run pytest`, `uv run pyright`, `uv run ruff check .`), but this is expected and correct for the final integration task — these ACs are themselves defined in design.md as whole-repo build gates, and T03 is explicitly sequenced last (`depends_on: ["T01", "T02"]`) to run after both prior tasks land.

### Visual Artifact Coverage
Not applicable. context.md's `## Visual Artifacts` section states "None," consistent with design.md (a CLI/config-only change with no UI surface, no mockups or screenshots referenced anywhere in design.md).

### context.md Completeness
Complete — all 5 required sections plus the optional `## Convention Examples` section are present and populated with content sourced from design.md (Key Decisions correctly compress the design's Architecture/Alternatives-Considered rationale; Constraints & Anti-Patterns correctly restates the Non-Goals and Key Constraints; Design Doc References point to the correct design.md sections).

### Identifier Format Violations
None. All FR and AC identifiers in both design.md and the task files use the exact `FR#N` / `AC#N` format.

## Minor Observations (non-blocking, not FR/AC gaps)

These do not affect the PASS verdict since they are not tied to a numbered FR/AC, but are worth noting for the implementer:

1. **`docs/configuration.md` line-number drift:** T03's Prompt says the HSL101 token families section starts "at line 94" and Focus cites "`docs/configuration.md:113`" for the field-reference sentence. Current file has the "no default token vocabulary" opening paragraph at line 96 and the field-reference sentence at line 113 (exact match on the second citation, 2-line drift on the first). Not a blocker — the surrounding grep context (`hash = "optional"` at line 105, "at most 32 families" language at line 113) makes the target unambiguous even with the 2-line drift.
2. **Design.md Edge Case "explicit `tokens = []` still raises `ConfigError`" has no dedicated regression test in any task.** This behavior is structurally preserved (the existing raw-array check at `config.py:341` is untouched by T01's changes, confirmed by reading the Prompt and current source), and no existing test in `test_config.py` currently exercises this exact scenario either (searched for `tokens = []`, `must contain 1 to`, `32 famil` — no matches). Since this edge case has no FR/AC identifier, its absence does not create a traceability gap, but it is a design-called-out behavior with no explicit test coverage added or preserved by name in any task's Verify/new-test list.
3. **Documentation changes (docs/configuration.md, README.md, docs/rules.md) have no dedicated Verify checklist items in T03** — only prose Prompt instructions (steps 3-5) and the Focus section's line pointers. Since none of design.md's FRs/ACs are documentation-specific, this doesn't create an FR/AC coverage gap, but it means there's no explicit, checkable acceptance criterion confirming the doc rewrites were done correctly beyond human/reviewer judgment.

## Summary

- Total FRs: 7 (FR#1-FR#7), all covered.
- Total ACs: 5 (AC#1-AC#5), all covered.
- 0 coverage gaps, 0 contradictions, 0 vague or non-local verification criteria, context.md complete.
- All 9 files listed in design.md's "Changed Files" section are covered by exactly one task's Target Files list, with no file owned by more than one task (no shared-mutable-state risk across T01/T02/T03).
- Dependency chain (T01 → T02 → T03, with T03 depending on both) is a valid, acyclic sequence matching the order in which the underlying code must change (data model → regex/detection → integration/docs).

**Verdict: PASS**
